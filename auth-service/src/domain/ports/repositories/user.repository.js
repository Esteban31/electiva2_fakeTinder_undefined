// const { connect } = require('../databases/mongoClient');

// const userRepositoryMongo = {
//   async findById(id) {
//     const db = await connect();
//     const user = await db.collection('users').findOne({ _id: id });
//     return user;
//   },
// };

// module.exports = userRepositoryMongo;
